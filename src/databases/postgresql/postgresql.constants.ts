import { CropId } from "./gameplay"

// Purpose: Constants for the gameplay-postgresql database.
export const CONNECTION_TIMEOUT_MS = 5000
export const POOL_SIZE = 10000

// default value for cropId
export const defaultCropId = CropId.Carrot