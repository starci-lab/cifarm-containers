podTemplate {
    node(POD_LABEL) {
        stage('Run kubectl') {
            withKubeConfig([credentialsId: 'jenkins-agent-token']) {
                sh 'kubectl get pods -A'
            }
        }
    }
}