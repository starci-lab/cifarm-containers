services:
  server:
    build:
      context: ../..
      dockerfile: apps/seed-static-data/Dockerfile
    env_file:
      - ../../.env.docker
    environment:
      NODE_ENV: production

#docker compose -f apps/seed-static-data/compose.yaml up -d