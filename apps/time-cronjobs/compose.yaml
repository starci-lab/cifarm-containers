services:
  server:
    build:
      context: ../..
      dockerfile: apps/time-cronjobs/Dockerfile
    env_file:
      - ../../.env.docker
    environment:
      NODE_ENV: production
    ports:
      - "3005:3005"

#docker compose -f apps/time-cronjobs/compose.yaml up -d